# `_check/` — 검사와 부품 지도

**이 폴더는 저장소가 제 코드를 재는 자들이 사는 자리다.** 지도가 따로 있어야 하는 까닭은 파일
이름이 답을 안 주기 때문이다 — 겉으로 다 같은 `python -X utf8 _check/<이름>.py` 인데 **초록의 뜻이
갈래마다 다르다.** 어떤 초록은 *우리 코드가 계약을 지킨다*는 말이고, 어떤 초록은 *저쪽이 그렇게
하더라*는 말이다. 둘을 섞으면 「다 통과했다」가 아무 뜻도 없는 말이 된다.

**한 검사가 무엇을 재나의 진본은 그 파일의 머리 첫 줄이다.** 표의 「무엇을 재나」 칸은 손으로 안
적고 거기서 파생한다 — 표 곁 표식이 그 자리를 말하고 `map_check.py` 가 어긋남을 문다. 표가 스스로
드는 것은 *무엇이 있고 언제 부르나*까지다.

## 초록의 뜻이 갈래마다 다르다

| 갈래 | 무엇을 부르나 | 무엇이 있어야 도나 | 초록이 뜻하는 것 |
|---|---|---|---|
| **게이트** | 우리 코드만 | 이 나무의 파이썬 | **우리가 정한 계약이 코드에 서 있다.** 저쪽이 무엇을 주는지는 아무 말도 안 한다 |
| **화면 실측** | 진짜 브라우저에 띄운 우리 화면 | 위 + node · playwright · 크로미움 | **화면이 실제로 그렇게 군다.** 소스에 문자열이 있나가 아니라 그려진 것을 읽는다 |
| **프로브** | 게이트웨이·진짜 CLI·진짜 모델 | 위 + 자격과 그 망 | **저쪽이 지금 그렇게 하더라.** 판마다 흔들릴 수 있어 한 판은 사실이지 규칙이 아니다 |
| **재는 자** | 갈래마다 다르다 | 그 줄이 든다 | **없다 — 판정을 안 낸다.** 수·견줌표만 내고 정하는 것은 사람이다 |

⚠ **갈래가 곧 「돌려도 되나」다.** 게이트와 화면 실측은 아무 때나 돌려도 잃을 것이 없다. 프로브는
**누르는 순간 값을 치른다.** 그래서 프로브는 「검사를 다 돌리자」에 안 들어간다.

## 종료코드 — 갈래를 코드가 말한다

훅도 CI 도 화면 문구를 안 읽고 코드만 본다. 그래서 이 갈림이 곧 계약이다.

| 코드 | 뜻 | 조심할 것 |
|---|---|---|
| **0** | 쟀고 맞다 | 「전부 통과」는 *여기까지* 통과다 — 무엇을 쟀나(건수·범위)를 초록 줄이 같이 찍는다 |
| **1** | 어긋났다 | 고침을 넣었는데 그대로 빨강이면 다음에 열 것은 대상이 아니라 **검사**다 |
| **2** | **못 쟀다** | 초록이 아니다. 자격·검체·브라우저가 없어 **아예 안 재인** 자리다 |

⚠ **`raise SystemExit("글자")` 는 2 를 1 로 뭉갠다** — 파이썬이 그 글자를 stderr 로 찍고 무조건 **1**
로 나간다. 화면은 맞는 말을 하는데 훅·CI 만 조용히 틀린다. 「못 쟀다」로 그 자리에서 나가는 손은
**한 벌**이다 — `raise Unmeasured("…")` 가 까닭을 찍고 2 로 나간다. 상수는 `_verdict.py` 의
`EXIT_OK`·`EXIT_MISMATCH`·`EXIT_UNMEASURED` 다. `sys.exit(main())` 꼴이면 `main` 의 `return` 도
그 상수로 한다 — 그 꼴까지 `exit_code_check.py` 가 문다.

## 게이트 — 바깥으로 안 나간다

<!-- generated(둘째 칸): python _check/map_check.py --write · 손으로 고치지 않는다 · 어긋남은 같은 스크립트가 기본으로 문다 · 진본은 그 검사 파일의 머리 첫 줄 · 나머지 칸은 사람 것이라 닫는 표식이 없다 -->
| 검사 | 무엇을 재나 | 언제 돌리나 (무엇을 만질 때) |
|---|---|---|
| `exit_code_check.py` | 검사가 종료코드를 계약에서 받나 — 뭉개는 문자열 · 계약 밖 값 · 손글자 2 · main 의 return 을 문다 | 검사를 새로 짓거나 나가는 자리를 고칠 때 · 커밋 전 |
| `verdict_line_check.py` | 초록 줄이 「몇 건을 쟀나」를 파생으로 찍나 — 판정을 세우고도 `passes()` 를 안 읽는 검사를 문다 | 검사를 새로 짓거나 초록 줄을 고칠 때 · 커밋 전 |
| `map_check.py` | 검사 지도가 실물과 맞나 — 줄 ↔ 파일의 차집합 · 「무엇을 재나」 칸은 머리 첫 줄에서 낸다 | 검사를 새로 짓거나 지우거나 이름을 바꿀 때 · **검사의 머리 첫 줄을 고칠 때** · `--gates` 로 태울 목록을 낸다(뺄 것은 곁 `map_check.conf` `[needs_arg]`) |
| `borrowed_check.py` | 빌려 온 사본이 진본과 같은가 — 머리말의 「빌려 온 자다 — 진본은 …」 곁말을 읽어 그 자리와 견준다 | 씨앗에서 받은 뒤 · 씨앗이 움직였다는 말을 들었을 때 · 커밋 전 |
| `part_map_check.py` | 부품 지도 문서의 표가 부품 폴더 실물과 맞나 — 양쪽 차집합 | 부품 지도 문서를 고칠 때 · 부품 폴더에 파일이 나거나 사라질 때 — **인자 셋**(지도 문서 · 부품 폴더 · 절 제목) 또는 곁 `part_map.conf` |
| `env_names_check.py` | 환경변수 문서의 이름이 코드가 **읽는** 이름과 맞나 — 양쪽 차집합 | 환경변수를 들이거나 지울 때 · 그 문서를 고칠 때 — **인자**(`--doc` · `--ours` 그물) 또는 곁 `env_names.conf` |
| `doc_span_check.py` | 문서의 백틱 조각이 실물을 가리키나 — 경로는 나무에, 식별자는 코드에 있나 | 문서를 옮기거나 파일 이름을 바꿀 때 · 코드 나무가 늘 때 — **인자**(나무 · 선언 `_check/doc_span.conf`, 씨앗은 안 싣는다) |
| `boundary_edge_check.py` | 외곽과 동일값으로 녹은 카드 행이 노출 모서리 처리를 실었나 — 인자로 준 HTML | 메일 지면 산출물 검수 · 카드 경계 조항을 고칠 때 — **인자**(`--width` · 산출물). 인자 없이는 자검만 |
| `script_syntax_check.py` | 산출물 HTML 의 인라인 스크립트가 문법에 맞나 — 깨진 것을 잡고 못 재면 못 쟀다고 한다 | 산출물 HTML 의 인라인 스크립트를 고칠 때 — **인자**(산출물). 인자 없이는 자검만 · node 가 든다 |
| `log_race_check.py` | 여럿이 한꺼번에 찍어도 줄이 안 섞이나 — 콘솔 tee 의 꼬리를 잰다 | 콘솔 tee · 로그 배선을 고칠 때 — **인자**(`--plumb` 배관). `--self` 는 배관 없이 자검 |
| `child_decode_check.py` | 자식 프로세스의 말이 부모에게 성히 건너오나 — 해독 계약을 실물 자식으로 잰다 | `subprocess` 를 부르는 자리를 고칠 때 — 인자 없이 기전만 · 파일을 주면 부모·자식 쪽 소스 전수 |

## 화면 실측 — 브라우저가 있어야 돈다

<!-- generated(둘째 칸): python _check/map_check.py --write · 손으로 고치지 않는다 · 어긋남은 같은 스크립트가 기본으로 문다 · 진본은 그 검사 파일의 머리 첫 줄 · 나머지 칸은 사람 것이라 닫는 표식이 없다 -->
| 검사 | 무엇을 재나 | 언제 돌리나 (무엇을 만질 때) |
|---|---|---|
| `shot_clip_check.py` | 긴 지면을 띠로 잘라 찍을 때 clip 이 **전체 쪽 좌표**인가 — playwright 의 축 | 긴 지면을 띠로 잘라 찍는 자리를 고칠 때 · playwright 판을 올릴 때 — playwright 파이썬 + 크로미움이 든다. 없으면 2 |

## 공용 부품 — 검사가 아니다

밑줄로 시작하는 파일은 검사가 아니라 여러 검사가 함께 쓰는 부품이다. **새 검사는 여기서 불러 쓰지
사본을 뜨지 않는다.**

| 부품 | 무엇을 드나 |
|---|---|
| `_verdict.py` | **판정 한 벌과 종료코드 계약** — 계약 판정 · 값 대조 · 통과할 때도 보여야 하는 경계 · 아예 안 잰 자리(`unmeasured()`) · 계수. 숫자 셋과 **그 자리에서 2 로 나가는 손**(`Unmeasured`)의 진본이 여기다 |
| `_serve.py` | 서버를 제 프로세스 안에 세우고(`stand`·`stop`) 문을 두드리는(`knock`·`jknock`) 한 벌 — 앱을 안 문다 |
| `_browser.mjs` | 브라우저를 판에 안 매이게 띄운다 — 실행 파일을 찾아 직접 가리키고 무엇을 집었나 찍는다 |
| `_tree.mjs` | 검사가 제 뿌리와 제 파이썬을 묻는 자리 — 자리를 박지 않고 물어서 받는다 |
| `_pixels.py` | 결정론 잡음 PNG — 「이름만 그림」인 가짜가 안 실리게 된 뒤의 검체 (PIL 이 든다) |
| `_bake.mjs` | 검사가 먹일 파일을 굽는다 — PNG · EMF · docx · DRM 머리. 바이너리 검체를 폴더에 안 둔다 · 머리 바이트는 부르는 쪽이 제 진본에서 읽어 넘긴다 |

## 이 폴더의 규율

1. **양성 대조 없이 음성 판정을 세우지 않는다.** 「안 일어난다」만 재면 화면이 통째로 안 떠도
   초록이 된다.
2. **재는 자가 곧 재이는 것이면 답은 늘 초록이다.** 한쪽을 진본으로 세우고 다른 쪽을 거기서 지어
   견준다.
3. **전수를 셀 때는 센티널을 같이 세운다.** 수집이 무너지면 `0 == 0` 으로 초록이 난다.
4. **판정마다 제 이빨을 문다** — 고침을 걷은 사본이 실제로 빨강을 내는지까지 본다.
5. **경계는 검사가 스스로 말한다.** 통과할 때도 보여야 하는 말은 `edge()` 로 — 실패 상세에 싣지
   않는다.
6. **초록 줄은 「몇 건을 쟀나」를 달고 나간다.** 그 건수를 손으로 적지 않는다 — `len(passes())` 가
   센다. `verdict_line_check.py` 가 문다.
7. **받아 간 사본은 손으로 고치지 않는다.** 곁말이 진본을 가리키고 `borrowed_check.py` 가 문다.
   고쳐야 했다면 그 고침은 진본으로 올린다.
8. **실물 결함을 만나면 여기서 고치지 않는다** — 이 폴더는 재는 자리다.

## 무엇을 안 재나

| 자리 | 왜 |
|---|---|
| `.mjs`·`.sh`·`.ps1` 의 종료코드 꼴과 초록 줄 | 무는 자 둘이 파이썬 AST 만 읽는다. `.mjs` 는 `results.length` 로 같은 규율을 지키되 재는 자가 없다 |
| 「못 쟀다」의 **갈래 선택** — 못 쟀는데 0 이나 1 로 나가는 자리 | 꼴이 아니라 뜻이라 사람이 읽는다 |
| 한국어 수사로 적은 범위(「계약 아홉」)가 맞는 말인가 | 글자가 곧 뜻이다 |
| 곁말 없이 베낀 사본 | 받는 자에게는 남이다 — 갈림을 세우는 자의 몫 |
| 어느 검사가 **실제로** 어느 코드로 나가나 | 원문의 꼴까지다 — 돌려 보지 않는다 |
